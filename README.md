# 🏥 Sistema de Gestión Hospitalaria (API REST)

Este proyecto consiste en una API REST desarrollada con **Node.js**, **Express** y **Sequelize** para la gestión de hospitales y sus plantas asociadas. Se ha implementado siguiendo una arquitectura por capas (Controladores, Servicios y Modelos).

## 🛠️ Configuración e Instalación

Sigue estos pasos para poner en marcha el proyecto en tu entorno local:

### 1. Requisitos previos
* Tener instalado **Node.js** y **npm**.
* Disponer de un servidor **MySQL** activo (XAMPP, WAMP o Docker).

### 2. Instalación de dependencias
En la raíz del proyecto, abre una terminal y ejecuta:
```bash
npm install

